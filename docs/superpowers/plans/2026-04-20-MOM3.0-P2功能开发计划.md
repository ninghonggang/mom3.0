# MOM3.0 P2功能开发计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补充完善P2级缺失功能，包括MES工单排程、WMS标签打印/策略配置、BPM任务转移/候选人、SCP QAD对接、EAM维修工单等

**Architecture:** 采用分层架构 (Handler → Service → Repository → Model)，遵循现有代码模式

**Tech Stack:** Go + Gin (后端), Vue3 + ElementPlus (前端), PostgreSQL (数据库)

---

## 一、P2功能清单

### 1.1 MES模块 - 工单排程

| 功能 | 设计文档路径 | 代码状态 | 优先级 |
|------|-------------|---------|--------|
| 工单排程主表 | `/mes/work-scheduling/*` | 缺失 | P2 |
| 工单排程明细 | `/mes/work-scheduling-detail/*` | 缺失 | P2 |
| 工序报工 | reportWorkByProcess | 缺失 | P2 |
| 工序完工 | processFinished | 缺失 | P2 |

### 1.2 WMS模块 - 标签打印/策略配置

| 功能 | 设计文档路径 | 代码状态 | 优先级 |
|------|-------------|---------|--------|
| 标签模板 | `/wms/label-template/*` | 缺失 | P2 |
| 标签打印历史 | `/wms/label-print-history/*` | 缺失 | P2 |
| 策略配置 | `/wms/strategy/*` | 缺失 | P2 |
| 策略规则 | `/wms/strategy-rule/*` | 缺失 | P2 |

### 1.3 BPM模块 - 任务转移/候选人

| 功能 | 设计文档路径 | 代码状态 | 优先级 |
|------|-------------|---------|--------|
| 任务转移 | `/bpm/task/transfer` | 缺失 | P2 |
| 候选人列表 | `/bpm/task/candidate` | 缺失 | P2 |
| 候选组列表 | `/bpm/task/candidate-group` | 缺失 | P2 |

### 1.4 SCP模块 - QAD对接

| 功能 | 设计文档路径 | 代码状态 | 优先级 |
|------|-------------|---------|--------|
| QAD数据同步 | `/scp/qad/sync` | 缺失 | P2 |
| QAD订单确认 | `/scp/qad/confirm` | 缺失 | P2 |
| QAD发货通知 | `/scp/qad/delivery` | 缺失 | P2 |

### 1.5 EAM模块 - 维修工单

| 功能 | 设计文档路径 | 代码状态 | 优先级 |
|------|-------------|---------|--------|
| 维修工单 | `/eam/repair-job/*` | 部分实现 | P2 |
| 维修流程配置 | `/eam/repair-flow/*` | 缺失 | P2 |
| 维修标准 | `/eam/repair-std/*` | 缺失 | P2 |

---

## 二、任务分解

### Task 18: MES工单排程主表和明细

**Files:**
- Create: `mom-server/internal/model/mes_work_scheduling.go`
- Create: `mom-server/internal/model/mes_work_scheduling_detail.go`
- Create: `mom-server/internal/repository/mes_work_scheduling.go`
- Create: `mom-server/internal/repository/mes_work_scheduling_detail.go`
- Create: `mom-server/internal/handler/mes/work_scheduling.go`
- Create: `mom-server/internal/service/mes_work_scheduling.go`
- Modify: `mom-server/internal/router/router.go`

**设计文档:** `docs/MOM3.0_MES生产执行模块设计文档.md` §工单排程

- [ ] 创建 `plan_mes_work_scheduling` 表 Model
  ```go
  type MesWorkScheduling struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      PlanNoDay       string    `gorm:"size:50"`      // 关联日计划单号
      SchedulingCode  string    `gorm:"size:50"`      // 工单编码
      ProductCode     string    `gorm:"size:50"`      // 产品编码
      ProductName     string    `gorm:"size:200"`     // 产品名称
      Status          string    `gorm:"size:20"`      // READYTODO/WAITBEGIN/PROCESSING/FINISH/TERMINALE
      Quantity        float64   `gorm:"type:decimal(18,2)"`  // 计划数量
      FinishedQty     float64   `gorm:"type:decimal(18,2)"`  // 完成数量
      WorkMode        string    `gorm:"size:20"`      // BATCH批量/单件
      TaskMode        string    `gorm:"size:20"`      // ASSIGN派工/RECIEVE领工
      PlanDate        time.Time `gorm:"type:date"`
      // ... 其他字段
  }
  ```

- [ ] 创建 `plan_mes_work_scheduling_detail` 表 Model
  ```go
  type MesWorkSchedulingDetail struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      SchedulingID    uint64    `gorm:"index"`        // 工单主表ID
      WorkingNode     string    `gorm:"size:50"`      // 工序编码
      WorkingName     string    `gorm:"size:100"`     // 工序名称
      Status          string    `gorm:"size:20"`      // 工序状态
      EquipmentID    uint64    `gorm:"index"`        // 设备ID
      WorkstationID  uint64    `gorm:"index"`        // 工位ID
      WorkerID       uint64    `gorm:"index"`        // 人员ID
      StartTime      *time.Time
      EndTime        *time.Time
      // ... 其他字段
  }
  ```

- [ ] 实现工单排程CRUD API
  - POST `/mes/work-scheduling/create` - 创建工单
  - PUT `/mes/work-scheduling/update` - 更新工单
  - DELETE `/mes/work-scheduling/delete` - 删除工单
  - GET `/mes/work-scheduling/get` - 获取工单
  - GET `/mes/work-scheduling/page` - 工单分页
  - POST `/mes/work-scheduling/publish` - 发布工单

- [ ] 实现工单明细CRUD API
  - POST `/mes/work-scheduling-detail/create`
  - PUT `/mes/work-scheduling-detail/update`
  - DELETE `/mes/work-scheduling-detail/delete`
  - GET `/mes/work-scheduling-detail/get`
  - GET `/mes/work-scheduling-detail/page`

- [ ] 实现工序操作API
  - PUT `/mes/work-scheduling-detail/start` - 工序开工
  - PUT `/mes/work-scheduling-detail/pause` - 工序暂停
  - PUT `/mes/work-scheduling-detail/resume` - 工序恢复
  - PUT `/mes/work-scheduling-detail/complete` - 工序完工
  - POST `/mes/work-scheduling-detail/report` - 工序报工
  - PUT `/mes/work-scheduling-detail/bindEquipment` - 绑定设备
  - PUT `/mes/work-scheduling-detail/bindWorker` - 绑定人员

- [ ] 注册路由到 router.go

---

### Task 19: WMS标签打印/策略配置

**Files:**
- Create: `mom-server/internal/model/wms_label_template.go`
- Create: `mom-server/internal/repository/wms_label_template.go`
- Create: `mom-server/internal/handler/wms/label_template.go`
- Create: `mom-server/internal/service/wms_label_template.go`
- Create: `mom-server/internal/model/wms_strategy.go`
- Create: `mom-server/internal/repository/wms_strategy.go`
- Create: `mom-server/internal/handler/wms/strategy.go`
- Create: `mom-server/internal/service/wms_strategy.go`
- Modify: `mom-server/internal/router/router.go`

**设计文档:** `docs/MOM3.0_WMS仓储模块设计文档.md` §标签打印/策略配置

- [ ] 创建标签模板表 `wms_label_template`
  ```go
  type WmsLabelTemplate struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      TemplateCode    string    `gorm:"size:50;uniqueIndex:idx_tenant_code"`
      TemplateName    string    `gorm:"size:100"`
      TemplateType    string    `gorm:"size:20"`  // ITEM/INVENTORY/LOCATION/Pallet
      Width           float64   `gorm:"type:decimal(10,2)"`
      Height          float64   `gorm:"type:decimal(10,2)"`
      Content         string    `gorm:"type:text"`  // JSON格式标签内容
      Status          string    `gorm:"size:20"`
  }
  ```

- [ ] 实现标签模板CRUD
  - POST `/wms/label-template/create`
  - PUT `/wms/label-template/update`
  - DELETE `/wms/label-template/delete`
  - GET `/wms/label-template/get`
  - GET `/wms/label-template/page`

- [ ] 创建策略配置表 `wms_strategy`
  ```go
  type WmsStrategy struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      StrategyCode    string    `gorm:"size:50;uniqueIndex:idx_tenant_code"`
      StrategyName    string    `gorm:"size:100"`
      StrategyType    string    `gorm:"size:20"`  // PICK/PUTAWAY/TRANSFER
      RuleContent     string    `gorm:"type:text"`  // JSON格式规则
      Priority        int       `gorm:"default:0"`
      Status          string    `gorm:"size:20"`
  }
  ```

- [ ] 实现策略配置CRUD
  - POST `/wms/strategy/create`
  - PUT `/wms/strategy/update`
  - DELETE `/wms/strategy/delete`
  - GET `/wms/strategy/get`
  - GET `/wms/strategy/page`
  - POST `/wms/strategy/apply` - 应用策略

- [ ] 注册路由

---

### Task 20: BPM任务转移/候选人

**Files:**
- Create: `mom-server/internal/model/bpm_task_transfer.go`
- Create: `mom-server/internal/repository/bpm_task_transfer.go`
- Create: `mom-server/internal/handler/bpm/task_transfer.go`
- Create: `mom-server/internal/service/bpm_task_transfer.go`
- Modify: `mom-server/internal/router/router.go`

**设计文档:** `docs/MOM3.0_BPM流程模块设计文档.md`

- [ ] 创建任务转移记录表 `bpm_task_transfer`
  ```go
  type BpmTaskTransfer struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      TaskID          string    `gorm:"size:64;index"`
      FromUserID      uint64    `gorm:"index"`
      FromUserName    string    `gorm:"size:100"`
      ToUserID        uint64    `gorm:"index"`
      ToUserName      string    `gorm:"size:100"`
      TransferReason  string    `gorm:"size:500"`
      TransferTime    time.Time
      OperatorID      uint64    // 操作人
  }
  ```

- [ ] 实现任务转移API
  - POST `/bpm/task/transfer` - 转移任务
    - 请求体: `{taskId, toUserId, reason}`
  - GET `/bpm/task/transfer/history/:taskId` - 转移历史

- [ ] 实现候选人/组API
  - GET `/bpm/task/candidate/:taskId` - 获取任务候选人列表
  - GET `/bpm/task/candidate-group/:taskId` - 获取候选组列表
  - POST `/bpm/task/assign` - 指定候选人

- [ ] 注册路由到 router.go

---

### Task 21: SCP QAD对接接口

**Files:**
- Create: `mom-server/internal/model/scp_qad_sync.go`
- Create: `mom-server/internal/repository/scp_qad_sync.go`
- Create: `mom-server/internal/handler/scp/qad.go`
- Create: `mom-server/internal/service/scp_qad.go`
- Modify: `mom-server/internal/router/router.go`

**设计文档:** `docs/MOM3.0_SCP供应链模块设计文档.md`

- [ ] 创建QAD同步记录表 `scp_qad_sync_log`
  ```go
  type ScpQadSyncLog struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      SyncType        string    `gorm:"size:20"`  // ORDER/DELIVERY/INVENTORY
      SyncDirection   string    `gorm:"size:10"`  // UPLOAD/DOWNLOAD
      QadDocNo        string    `gorm:"size:50"`
      MomDocNo        string    `gorm:"size:50"`
      Status          string    `gorm:"size:20"`  // PENDING/SUCCESS/FAILED
      RequestContent  string    `gorm:"type:text"`
      ResponseContent string    `gorm:"type:text"`
      ErrorMsg        string    `gorm:"size:500"`
      SyncTime        time.Time
  }
  ```

- [ ] 实现QAD同步API
  - POST `/scp/qad/sync` - 同步数据到QAD
    - 请求体: `{syncType, docNo, data}`
  - GET `/scp/qad/sync/status/:syncId` - 同步状态
  - GET `/scp/qad/sync/log/:docNo` - 同步日志

- [ ] 实现QAD订单确认回调
  - POST `/scp/qad/confirm` - QAD订单确认回调
  - POST `/scp/qad/delivery` - QAD发货通知回调

- [ ] 注册路由到 router.go

---

### Task 22: EAM维修工单/流程/标准

**Files:**
- Create: `mom-server/internal/model/eam_repair_job.go`
- Create: `mom-server/internal/repository/eam_repair_job.go`
- Create: `mom-server/internal/handler/eam/repair_job.go`
- Create: `mom-server/internal/service/eam_repair_job.go`
- Create: `mom-server/internal/model/eam_repair_flow.go`
- Create: `mom-server/internal/model/eam_repair_std.go`
- Modify: `mom-server/internal/router/router.go`

**设计文档:** `docs/MOM3.0前端_EAM设备管理模块设计文档.md`

- [ ] 创建维修工单表 `eam_repair_job`
  ```go
  type EamRepairJob struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      JobCode         string    `gorm:"size:50;uniqueIndex:idx_tenant_code"`
      EquipmentID     uint64    `gorm:"index"`
      EquipmentCode   string    `gorm:"size:50"`
      FaultType       string    `gorm:"size:30"`
      FaultReason     string    `gorm:"size:30"`
      FaultDesc       string    `gorm:"size:500"`
      Level           string    `gorm:"size:20"`  // URGENT/HIGH/NORMAL/LOW
      Status          string    `gorm:"size:20"`  // PENDING/ASSIGNED/IN_PROGRESS/COMPLETED/CANCELLED
      ReporterID      uint64
      ReporterName    string    `gorm:"size:100"`
      AssigneeID      uint64
      AssigneeName    string    `gorm:"size:100"`
      PlanStartTime   *time.Time
      PlanEndTime     *time.Time
      ActualStartTime *time.Time
      ActualEndTime   *time.Time
      Result          string    `gorm:"size:500"`
      Evaluation      string    `gorm:"size:20"`
  }
  ```

- [ ] 实现维修工单CRUD
  - POST `/eam/repair-job/create`
  - PUT `/eam/repair-job/update`
  - DELETE `/eam/repair-job/delete`
  - GET `/eam/repair-job/get`
  - GET `/eam/repair-job/page`
  - POST `/eam/repair-job/assign` - 派工
  - POST `/eam/repair-job/accept` - 接单
  - POST `/eam/repair-job/complete` - 完工
  - POST `/eam/repair-job/evaluate` - 评价

- [ ] 创建维修流程表 `eam_repair_flow`
  ```go
  type EamRepairFlow struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      FlowCode        string    `gorm:"size:50;uniqueIndex:idx_tenant_code"`
      FlowName        string    `gorm:"size:100"`
      FlowSteps       string    `gorm:"type:text"`  // JSON格式步骤
      Status          string    `gorm:"size:20"`
  }
  ```

- [ ] 创建维修标准表 `eam_repair_std`
  ```go
  type EamRepairStd struct {
      ID              uint64    `gorm:"primaryKey"`
      TenantID        int64     `gorm:"index"`
      StdCode         string    `gorm:"size:50;uniqueIndex:idx_tenant_code"`
      StdName         string    `gorm:"size:100"`
      FaultType       string    `gorm:"size:30"`
      RepairSteps     string    `gorm:"type:text"`  // JSON格式维修步骤
      ToolsRequired   string    `gorm:"size:500"`
      MaterialsRequired string   `gorm:"size:500"`
      StandardHours   float64
      Status          string    `gorm:"size:20"`
  }
  ```

- [ ] 注册路由

---

## 三、工作量估算

| Task | 描述 | 优先级 | 预计工时 |
|------|------|--------|---------|
| Task 18 | MES工单排程 (主表+明细+工序操作) | P2 | 2-3天 |
| Task 19 | WMS标签打印/策略配置 | P2 | 2天 |
| Task 20 | BPM任务转移/候选人 | P2 | 1-2天 |
| Task 21 | SCP QAD对接接口 | P2 | 1-2天 |
| Task 22 | EAM维修工单/流程/标准 | P2 | 2-3天 |

**P2工作量**: 约8-12个工作日

---

## 四、当前状态

- **已完成**: Task 1-22 (P1+P2任务全部完成) ✅
- **代码已提交**: 等待git push到远程
- **累计总工作量**: 约30-42个工作日

---

## 五、后续可能的P3功能

| 功能 | 说明 | 优先级 |
|------|------|--------|
| MES SOP-PDF管理 | 工艺文档电子化 | P3 |
| MES 报工管理 | 工序报工完整流程 | P3 |
| WMS 库区管理 | 仓库区域管理 | P3 |
| WMS 货品管理 | 物料主数据 | P3 |
| BPM 任务委托 | 任务转交功能 | P3 |